NEXT_PUBLIC_SUPABASE_URL="https://your-project-id.supabase.co"
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY="your-anon-publishable-key"
NEXT_PUBLIC_SUPABASE_PROJECT_ID="your-project-id"

# Notes:
# - Replace the values above with your Supabase project's values.
# - Never commit your real `.env` file to source control. Use this file as a template.

# Server-only (do NOT expose these in client-side envs):
SUPABASE_URL="https://your-project-id.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"
# AI provider keys (choose one or provide both). The function supports LOVABLE gateway or OpenAI.
# LOVABLE_API_KEY is used with the Lovable AI Gateway (https://ai.gateway.lovable.dev)
# OPENAI_API_KEY can be used as a fallback to call OpenAI's Chat Completions API.
LOVABLE_API_KEY="your-lovable-api-key"
OPENAI_API_KEY="your-openai-api-key"
